import type { FC } from "react";

const Header: FC = () => {
  return (
    <header className="py-3 text-2xl font-bold text-center">
      ARK ダメージシミュレーター
    </header>
  );
};

export default Header;
